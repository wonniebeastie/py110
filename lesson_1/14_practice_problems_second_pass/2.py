numbers = {1, 2, 3, 4, 5, 5, 4, 3}

print(len(numbers)) # 5

# What is the set's length? Try to answer without running the code.

# The set's length will be 5, because duplicates are not allowed in sets. So
# 3, 4, and 5 will be counted only once, each.
